---
id: toolchain/typescript
lang: en
version: 1
source-lang: en
status: active
digest: 2b66cd7b
---

# TypeScript Toolchain

The standard TypeScript / JavaScript toolchain: pnpm for package management, oxlint for linting, oxfmt for formatting. For framework selection, see [Frontend Framework: Vite vs Next.js](../libraries/frontend-framework.md).

## pnpm

Use `pnpm`.

This applies to normal Node.js projects and monorepos.

When external documentation provides npm or yarn commands, adapt them to pnpm rather than introducing another package manager.

The project lockfile must remain consistent with pnpm.

### Rejected Alternatives

Do not use:

* npm
* yarn

## oxlint

`oxlint` is the standard TypeScript / JavaScript linter.

When a framework, template, or library recommends ESLint:

1. Identify which ESLint rules or plugins it relies on.
2. Determine whether the required functionality is supported by oxlint.
3. Use the oxlint equivalent whenever possible.
4. Do not keep ESLint merely because it was generated by a template.

Use the latest stable oxlint version unless a concrete compatibility issue requires otherwise.

### Rule Configuration

Configure oxlint intentionally.

Enable the rules and recommended rule sets that are appropriate and officially supported by oxlint.

Do not leave useful recommended checks disabled simply because the generated project configuration does not enable them.

When a rule set or compatibility layer is not supported correctly by oxlint, configure it explicitly rather than attempting to force incompatible ESLint behavior into the project.

### Type-Aware / Type Checking Rules

Use oxlint's officially supported TypeScript type-aware or type-checking capabilities when they are appropriate for the project.

Follow the current official oxlint documentation to configure the required type-aware functionality.

Do not introduce ESLint solely to obtain type-aware linting if oxlint provides the required capability.

The exact configuration may evolve with oxlint, so prefer the current official oxlint mechanism rather than copying obsolete configuration from older projects.

### Rejected Alternatives

Do not use ESLint as the default linting system.

## oxfmt

`oxfmt` is the standard formatter for TypeScript / JavaScript projects.

Use the latest stable oxfmt version unless a concrete compatibility problem prevents it.

Do not run multiple competing formatters over the same source files.

### Rejected Alternatives

Do not use Prettier.
